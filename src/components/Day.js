import React from 'react'

export default function Day({day}) {
    console.log(day)
    return (
        <div>
            {day.name}
        </div>
        // button for expanding a page to 

    )
}
